[www]
; Allow multiple concurrent lookups
pm = dynamic
pm.max_children = 15
pm.start_servers = 4
pm.min_spare_servers = 2
pm.max_spare_servers = 8

; Long timeout for lookups (5 minutes)
request_terminate_timeout = 300
